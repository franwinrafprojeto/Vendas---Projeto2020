@model IEnumerable<Vendas.Models.CadastroEntregador>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome_Entregador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone_Entregador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GetRG_Entregador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Habilitação_Entregador)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome_Entregador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone_Entregador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GetRG_Entregador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Habilitação_Entregador)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
