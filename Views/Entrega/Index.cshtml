@model IEnumerable<Vendas.Models.Entrega>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_Entregador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_Endereco)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_Entregador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_Endereco)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
